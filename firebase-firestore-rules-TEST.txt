rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGOLE DI TEST: permettono accesso agli utenti autenticati anonimi.
    // NON usare come regole definitive per una palestra vera.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
